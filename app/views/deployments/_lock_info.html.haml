%p WARNING: The stage is locked.

- if stage.locking_deployment
  %p
    This stage is being deployed by
    = link_to stage.locking_deployment.user.login,
      stage.locking_deployment.user
    who executed
    = link_to stage.locking_deployment.task,
      [current_project, current_stage, stage.locking_deployment]
    at
    = stage.locking_deployment.created_at.to_s(:log)

  %p
    Override locking and deploy anyhow?
    %input{:type=>"checkbox", :id=>"override_locking_trigger", :value=>"1"}
